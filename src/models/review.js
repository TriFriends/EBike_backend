import mongoose from 'mongoose'
const { Schema } = mongoose

const reviewSchema = new Schema({
    user_identifier: {

    },
    grade: {

    },
    content: {
        
    }
})

export {
    reviewSchema
}